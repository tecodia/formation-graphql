version: "3"

services:
  postgres:
    image: postgres
    environment:
      POSTGRES_USER: root
      POSTGRES_PASSWORD: root
      POSTGRES_DB: formationGraphql
    ports:
      - "5432:5432"
  redis:
    image: "redis"
    ports:
      - "6379:6379"
